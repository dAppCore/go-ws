version: "2"

run:
  timeout: 5m
  go: "1.26"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - gocritic
  disable:
    - exhaustive
    - wrapcheck

issues:
  max-same-issues: 0

formatters:
  enable:
    - gofmt
